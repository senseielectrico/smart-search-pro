================================================================================
                    EXPORT SYSTEM ARCHITECTURE
================================================================================

┌─────────────────────────────────────────────────────────────────────────────┐
│                           USER INTERFACE LAYER                               │
└─────────────────────────────────────────────────────────────────────────────┘

    ┌──────────────────┐     ┌──────────────────┐     ┌──────────────────┐
    │   Main Window    │     │  Results Panel   │     │  Context Menu    │
    │   (main_window)  │────▶│ (results_panel)  │────▶│ (right-click)    │
    └──────────────────┘     └──────────────────┘     └──────────────────┘
            │                         │                         │
            │  Menu Bar               │  Toolbar                │  Signals
            │  - Export >             │  - 💾 Export            │  - export_requested
            │    • All (Ctrl+E)       │  - 📄 Clipboard         │  - export_selected_requested
            │    • Selected           │                         │
            │    • Quick CSV          │  Data Methods           │
            │    • Clipboard          │  - get_all_files()      │
            │                         │  - get_selected_files() │
            └─────────────┬───────────┴─────────────────────────┘
                          │
                          │ Opens Dialog
                          ▼
    ┌─────────────────────────────────────────────────────────────────────┐
    │                        EXPORT DIALOG                                 │
    │                     (ui/export_dialog.py)                            │
    │                                                                      │
    │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌───────────┐ │
    │  │  CSV Radio  │  │ Excel Radio │  │ HTML Radio  │  │ JSON/Clip │ │
    │  └─────────────┘  └─────────────┘  └─────────────┘  └───────────┘ │
    │                                                                      │
    │  ┌────────────────────────────────────────────────────────────────┐ │
    │  │                    Options Tabs                                │ │
    │  │  ┌──────────────┐  ┌──────────────────────────────────────┐   │ │
    │  │  │   General    │  │      Format-Specific Options         │   │ │
    │  │  │              │  │  - CSV: delimiter, encoding           │   │ │
    │  │  │ • Columns    │  │  - Excel: sheets, formatting          │   │ │
    │  │  │ • Headers    │  │  - HTML: theme, interactivity         │   │ │
    │  │  │ • Max Results│  │  - JSON: pretty, metadata             │   │ │
    │  │  │ • Date Format│  │  - Clipboard: format, separator       │   │ │
    │  │  │ • Size Format│  │                                        │   │ │
    │  │  └──────────────┘  └──────────────────────────────────────┘   │ │
    │  └────────────────────────────────────────────────────────────────┘ │
    │                                                                      │
    │  ┌────────────────────────────────────────────────────────────────┐ │
    │  │  Output: [/path/to/file.ext]  [Browse...]                     │ │
    │  └────────────────────────────────────────────────────────────────┘ │
    │                                                                      │
    │  [Cancel]                                             [Export]      │
    └──────────────────────────────────────┬───────────────────────────────┘
                                           │
                                           │ Builds Config
                                           │ Creates Exporter
                                           │ Starts Worker
                                           ▼
    ┌─────────────────────────────────────────────────────────────────────┐
    │                        EXPORT WORKER (QThread)                       │
    │                                                                      │
    │  Background execution with progress tracking                        │
    │  • Signals: progress_update, export_complete, export_error          │
    │  • Cancellable operation                                            │
    └──────────────────────────────────────┬───────────────────────────────┘
                                           │
                                           │ Calls export()
                                           ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                          BACKEND EXPORT LAYER                                │
└─────────────────────────────────────────────────────────────────────────────┘

    ┌───────────────────────────────────────────────────────────────────┐
    │                     BaseExporter (Abstract)                        │
    │                      (export/base.py)                              │
    │                                                                    │
    │  • ExportConfig - Configuration dataclass                          │
    │  • ExportStats - Result statistics                                 │
    │  • Common methods:                                                 │
    │    - _prepare_export()  - Filter and prepare data                  │
    │    - _format_value()    - Format individual values                 │
    │    - _report_progress() - Progress callbacks                       │
    │    - _batch_process()   - Batch processing                         │
    └─────────────────┬──────────────────────────────────────────────────┘
                      │
                      │ Inherited by:
                      │
        ┌─────────────┼─────────────┬─────────────┬─────────────┬──────────────┐
        │             │             │             │             │              │
        ▼             ▼             ▼             ▼             ▼              ▼
    ┌────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌────────────┐  ┌─────┐
    │  CSV   │  │  Excel   │  │   HTML   │  │   JSON   │  │ Clipboard  │  │ ... │
    │Exporter│  │ Exporter │  │ Exporter │  │ Exporter │  │  Exporter  │  │     │
    └────────┘  └──────────┘  └──────────┘  └──────────┘  └────────────┘  └─────┘
        │             │             │             │             │
        │             │             │             │             │
        ▼             ▼             ▼             ▼             ▼
    .csv file   .xlsx file    .html file   .json file    Clipboard


================================================================================
                            DATA FLOW DIAGRAM
================================================================================

Search Query
    │
    ▼
┌──────────────────┐
│  Search Engine   │
│  (backend.py)    │
└─────────┬────────┘
          │
          │ Results
          ▼
┌──────────────────┐          ┌────────────────────────────────────┐
│ Results Panel    │          │ Data Structure:                    │
│ (Virtual Table)  │──────────│ {                                  │
│                  │          │   'name': 'file.txt',              │
│ • VirtualTable   │          │   'path': '/home/user/docs',       │
│   Model          │          │   'full_path': '/home/user/...', │
│ • Row Caching    │          │   'extension': '.txt',             │
│ • Lazy Loading   │          │   'size': 1024,                    │
└─────────┬────────┘          │   'date_modified': 1234567890,     │
          │                   │   'date_created': 1234567890,      │
          │                   │   'is_folder': False               │
          │                   │ }                                  │
          │                   └────────────────────────────────────┘
          │
          │ get_all_files() or get_selected_files()
          ▼
┌──────────────────┐
│  Export Dialog   │
│                  │
│  1. User selects │
│     format       │
│  2. Configures   │
│     options      │
│  3. Clicks Export│
└─────────┬────────┘
          │
          │ ExportConfig + Results
          ▼
┌──────────────────┐
│  Export Worker   │
│  (Background)    │
│                  │
│  • Progress      │───────▶ Progress Dialog
│  • Cancellable   │
└─────────┬────────┘
          │
          │ exporter.export(results)
          ▼
┌──────────────────┐
│  Exporter        │
│  (CSV/Excel/     │
│   HTML/JSON/     │
│   Clipboard)     │
│                  │
│  1. Validate     │
│  2. Format data  │
│  3. Write output │
│  4. Return stats │
└─────────┬────────┘
          │
          ▼
┌──────────────────┐
│  Output          │
│  • File on disk  │
│  • Clipboard     │
│  • ExportStats   │
└─────────┬────────┘
          │
          ▼
User Notification
    "Export complete!
     500 files exported."


================================================================================
                        SIGNAL/SLOT CONNECTIONS
================================================================================

MainWindow
    │
    ├─ Menu Action: "Export All" (triggered)
    │     └──▶ _export_results()
    │
    ├─ Menu Action: "Export Selected" (triggered)
    │     └──▶ _export_selected()
    │
    ├─ Menu Action: "Quick CSV" (triggered)
    │     └──▶ _quick_export_csv()
    │
    ├─ Menu Action: "Clipboard" (triggered)
    │     └──▶ _export_to_clipboard()
    │
    └─ ResultsPanel Signals
          ├─ export_requested
          │     └──▶ _export_results()
          │
          └─ export_selected_requested
                └──▶ _export_selected()

ExportDialog
    │
    ├─ Format RadioButton (clicked)
    │     └──▶ _on_format_changed()
    │           └──▶ Updates options tab
    │
    ├─ Browse Button (clicked)
    │     └──▶ _browse_output()
    │           └──▶ QFileDialog
    │
    └─ Export Button (clicked)
          └──▶ _start_export()
                ├──▶ Creates exporter
                ├──▶ Creates worker thread
                ├──▶ Shows progress dialog
                └──▶ Starts export

ExportWorker (QThread)
    │
    ├─ progress_update (emitted)
    │     └──▶ ProgressDialog.setValue()
    │
    ├─ export_complete (emitted)
    │     └──▶ _on_export_complete()
    │           ├──▶ Close progress
    │           ├──▶ Show success message
    │           └──▶ Optional: open file
    │
    └─ export_error (emitted)
          └──▶ _on_export_error()
                ├──▶ Close progress
                └──▶ Show error message


================================================================================
                    CONFIGURATION FLOW
================================================================================

User Input (Dialog)
    │
    ├─ Format: CSV
    │     └─▶ Options:
    │           - delimiter: ","
    │           - encoding: "utf-8-sig"
    │           - excel_compatible: true
    │
    ├─ Format: Excel
    │     └─▶ Options:
    │           - freeze_panes: true
    │           - auto_filter: true
    │           - include_summary: true
    │           - split_by: "extension"
    │
    ├─ Format: HTML
    │     └─▶ Options:
    │           - theme: "auto"
    │           - sortable: true
    │           - searchable: true
    │
    ├─ Format: JSON
    │     └─▶ Options:
    │           - pretty: true
    │           - indent: 2
    │           - include_metadata: true
    │
    └─ Format: Clipboard
          └─▶ Options:
                - format: "csv"
                - separator: "\n"
    │
    ▼
ExportConfig Object
    {
        output_path: Path("/path/to/file.ext"),
        overwrite: true,
        columns: ["filename", "path", "size", ...],
        include_headers: true,
        max_results: None,
        date_format: "%Y-%m-%d %H:%M:%S",
        size_format: "human",
        options: { format-specific dict }
    }
    │
    ▼
Exporter Constructor
    │
    ▼
Configured Exporter Instance
    │
    ▼
export(results) method called
    │
    ▼
ExportStats Object
    {
        total_records: 500,
        exported_records: 500,
        skipped_records: 0,
        errors: 0,
        duration_seconds: 0.45,
        output_file: Path("/path/to/file.ext"),
        output_size_bytes: 45678
    }


================================================================================
                        ERROR HANDLING CHAIN
================================================================================

User Action
    │
    ▼
Try Export
    │
    ├─ No Results?
    │     └──▶ QMessageBox.warning("No results to export")
    │           └──▶ Return early
    │
    ├─ Missing Dependency? (openpyxl, pyperclip)
    │     └──▶ QMessageBox.critical("Install package")
    │           └──▶ Return early
    │
    ├─ Invalid Output Path?
    │     └──▶ QMessageBox.warning("Select output file")
    │           └──▶ Return early
    │
    ├─ Export Error?
    │     └──▶ catch Exception
    │           └──▶ QMessageBox.critical("Export failed: {error}")
    │                 └──▶ Show detailed error
    │
    └─ Success!
          └──▶ QMessageBox.information("Export complete!")
                └──▶ Optional: os.startfile(output_file)


================================================================================
                        KEYBOARD SHORTCUTS
================================================================================

Ctrl+E          ──▶  Export All Results (full dialog)
Ctrl+Shift+E    ──▶  Export Selected Results (full dialog)
Ctrl+Shift+C    ──▶  Copy to Clipboard (quick action)


================================================================================
                        PERFORMANCE OPTIMIZATION
================================================================================

Virtual Scrolling (Results Panel)
    │
    └─▶ Load only visible rows
          └─▶ Cache 2000 rows
                └─▶ Prune cache on scroll
                      └─▶ Minimal memory footprint

Batch Processing (Exporters)
    │
    └─▶ Process 500-1000 rows at a time
          └─▶ Report progress periodically
                └─▶ Responsive UI

Background Thread (ExportWorker)
    │
    └─▶ Non-blocking export operation
          └─▶ UI remains responsive
                └─▶ Can cancel operation

Efficient Data Structures
    │
    └─▶ Results passed by reference
          └─▶ No data copying
                └─▶ Direct access to model data


================================================================================
